import { FormConfig } from '../../../types/formConfig'

// Custom override for form config. Keep box empty to fallback to default form.
export const demoJpTestCustomFormConfig: FormConfig = {
  width: 100,
  box: [],
}
