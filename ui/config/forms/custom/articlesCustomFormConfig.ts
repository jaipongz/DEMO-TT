import { FormConfig } from '../../../types/formConfig'

// Custom override for Articles form.
// Keep box empty to fall back to default config. Add boxes/fields to override.
export const articlesCustomFormConfig: FormConfig = {
  width: 100,
  box: [],
}
 